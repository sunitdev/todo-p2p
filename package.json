{
  "name": "todo-p2p",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b && bun --filter @todo-p2p/web typecheck",
    "lint": "eslint .",
    "test": "bun --filter './packages/*' test",
    "hooks:install": "pre-commit install --install-hooks && pre-commit install --hook-type pre-push",
    "hooks:run": "pre-commit run --all-files"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/bun": "1.1.14",
    "@eslint/js": "9.15.0",
    "eslint": "9.15.0",
    "typescript-eslint": "8.15.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
