[package]
name = "todo-p2p-desktop"
version = "0.0.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# TODO: add when implementing native sync/storage
# iroh = "..."
# rusqlite = { version = "...", features = ["bundled-sqlcipher"] }
# keyring = "..."
