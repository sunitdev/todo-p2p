{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal scaffold capabilities. Add iroh:* / storage:* later when commands exist.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
