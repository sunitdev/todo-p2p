/**
 * Build-time app constants. Single source for the external links surfaced in
 * the UI (P9.2 "View source", P9.3 release download).
 *
 * TODO M6 (release engineering): replace these placeholders with the real
 * GitHub repository + releases URLs once the public repo is live.
 */

/** Public source repository — opened from Settings → "View source" (web only). */
export const SOURCE_URL = 'https://github.com/todo-p2p/todo-p2p';

/** Signed desktop release page — linked from the Unsupported screen. */
export const RELEASE_URL = 'https://github.com/todo-p2p/todo-p2p/releases';
